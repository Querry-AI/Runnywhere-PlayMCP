# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

Runnywhere(러니웨어) — 서울 보행 도로망 위에 러닝 코스와 동물 모양 GPS 아트 코스를 생성하는 **MCP 서버**. 카카오 PlayMCP 공모전 출품작이며, PlayMCP 심사 기준(stateless, p99 ≤ 3s, avg ≤ 100ms)이 설계 전반을 지배한다. 사용자 대면 텍스트는 전부 한국어, 코드 주석은 영어다. README.md에 데이터 스냅숏·환경변수 전체 목록이 있다.

## 명령어

```bash
source .venv/bin/activate && pip install -e '.[dev]'
python -m runart.server                  # http://localhost:8000/mcp (Streamable HTTP, JSON)
pytest                                   # 전체 (실제 58MB 그래프 로드 — 첫 테스트가 느림)
pytest 'tests/test_shapes.py::test_shape_course_passes_similarity_gate[dog]' -q  # 단일 테스트
pytest -k "zigzag or detour" -q          # 이름으로 골라 실행
python scripts/loadtest.py 1000 10       # 서버 구동 상태에서 부하 테스트
RUNART_LOADTEST_REPORT=artifacts/playmcp-loadtest.json python scripts/loadtest.py 1000 10
docker build -t runnywhere . && docker run -p 8000:8000 -e RUNART_BASE_URL=https://<endpoint> runnywhere
```

ETL(그래프·RFS·프리셋 재생성)은 `pip install -e '.[etl]'` 후 README.md "실행" 절의 순서를 그대로 따른다. 오래 걸리고 결과물이 코드의 체크섬과 묶여 있으므로, 요청받지 않았다면 실행하지 않는다.

## 아키텍처

의존 방향은 단방향이다: `geo` → `graph`/`rfs`/`facilities`/`infrastructure` → `course` → `shapes` → `server`. `render`·`widget`·`insights`·`exploration`은 표현 계층이고 `models`는 전역 공유된다. 역방향 import를 만들지 말 것.

- **`server.py`** — MCP 툴 9개 + 웹 라우트 17개를 한 앱에서 서빙. FastMCP `stateless_http=True, json_response=True`.
- **`course.py`** — 목표 거리의 원주에 waypoint 4개를 놓고 RFS 가중 Dijkstra로 잇는 순환 코스. 여러 bearing을 시도하고 ±5% 안에 들 때까지 원을 rescale. 후보 순위에는 RFS·경사·따라가기 쉬움과 함께 `retrace_share`(같은 길 되돌아오는 비율)가 들어간다 — 공원 보행망은 격자가 아니라 나무 구조라 이게 없으면 왕복 코스가 뽑힌다.
- **`shapes.py`** — 동물 템플릿(닫힌 폴리라인)을 스케일·회전·스냅한 뒤 유사도 게이트로 판정. 게이트를 못 넘으면 **나쁜 코스를 내보내는 대신 거절**하고 대안을 제안한다. 게이트 값은 동물마다 다르다(`SHAPE_STYLES`).
- **`rfs.py`** — 러닝 친화도 점수(6개 항목 가중합) + 라우팅 가중치. `night_mode`는 별도 프로파일, `include_hills`는 경사 항을 감점에서 가점으로 뒤집는다.
- **`insights.py`** — 코스가 스스로에 대해 말하는 것(특징 칩·좋은 점·참고할 점). 파생 문장은 전부 코스가 이미 가진 데이터(RFS 성분·누적 오르막·횡단 수·편의시설)에서만 만든다. 성분 임계값은 **서울 전역 실측 중앙값 기준**이라 절대 수치가 아니다 — 조명은 도시 전체가 무데이터 기본값 0.5 아래라, 고정 컷오프를 쓰면 "조명 어두움"이 네 코스 중 셋에 붙는다.
- **`etl/`** — 오프라인 파이프라인. 런타임에는 경로·안전·시설 데이터를 위해 외부 API를 절대 호출하지 않는다(지오코딩만 예외).

## 반드시 지켜야 할 불변식

**Stateless.** DB도 세션도 없다. `course_id`는 파라미터 전체를 zlib+base64로 담은 자기완결형 토큰이라 어느 인스턴스에서든 같은 코스를 재생성한다(`models.py`). passport·relay 토큰은 `RUNART_TOKEN_SECRET`으로 HMAC 서명한다(`exploration.py`). 서버에 상태를 두는 기능을 추가하지 말 것. `server.py`의 캐시는 순수 성능 계층이며, 실패(`CourseError`)도 결정적이므로 함께 캐싱한다.

**`CourseParams`에 필드를 추가하면 모든 기존 링크가 깨진다.** `canonical()`이 course_id를 만든다. 새 옵션이 필요하면 먼저 기존 필드로 표현할 수 없는지 보라 — 공원 라우팅은 `need_facilities=["park"]`를 옵트인 신호로 재사용해 id 호환을 지켰다. 불가피하면 `manual_waypoints`·`custom_name`처럼 기본값일 때 `canonical()`에서 `pop`한다.

**course_id는 코스를 *재생성*한다 — 그래서 생성 가중치를 바꾸면 이미 공유된 모든 링크의 경로가 조용히 달라진다.** 일반 코스 id에는 파라미터만 들어 있고 경로는 들어 있지 않다. `easy_route_weight()`에 이름 있는 보행로 선호를 넣었더니 서울시청·여의도 등 8개 출발지 경로가 전부 바뀌었다. 그래서 그 선호는 `prefer_named_walkways=True`를 넘기는 **편집 경로 전용**이다(편집본은 `manual_path`로 경로 자체를 id에 담으므로 안전하다). 생성 가중치를 건드릴 땐 먼저 대표 출발지 8곳의 경로가 그대로인지 확인하라.

**지연 예산.** `MCP_OUTER_RESPONSE_BUDGET_S = 2.85` > `ANIMAL/GENERAL_RESPONSE_BUDGET_S = 2.65` > `ADDRESS_TRY_BUDGET_S = 1.8`의 중첩 구조다. 툴 본문은 남은 시간을 `remaining()`으로 계산해 하위 호출에 넘긴다. 새 CPU 작업을 추가하면 자체 wall-clock 데드라인을 갖게 하고 `_intrinsically_bounded()`의 화이트리스트에 등록해야 한다 — 등록되지 않은 함수는 프로세스 풀이 죽었을 때 인라인 폴백 없이 타임아웃 처리된다. **하위 예산을 조일 땐 그 아래 알고리즘의 자체 상한을 먼저 확인하라**: 도형 탐색은 2.2s로 설계됐는데 주소 경로가 0.8s로 끊고 있어 임의 주소 동물 코스가 절반 이상 실패했다.

**툴은 예외를 던지지 않는다.** 모든 실패는 다음 행동을 알려주는 한국어 안내 문자열로 반환한다(`⚠️`/`⏱️` 접두). 실패 원인마다 다른 문구를 쓴다 — 하나의 포괄 메시지는 원인을 감춘다(`server._payload_problem`).

**툴 등록.** `server.py` 하단의 루프에서 `mcp.add_tool(offloaded(fn), ...)`으로 등록한다. 동기 함수 원본은 그대로 남아 테스트가 `server.generate_running_course(...)`처럼 직접 호출한다. 툴을 추가하려면 동기 함수를 정의하고 그 튜플에 한 줄을 더한다. `offloaded()`는 이벤트 루프를 헬스체크용으로 비워두는 장치이므로 벗기지 말 것.

**툴 설명문에는 서비스명 전체**(`Runnywhere(러니웨어: 어디서든 러닝 코스 짜기!)`)를 넣는다 — PlayMCP 노출 요건이다(커밋 7242382). 출발지 설명은 `LOCATION_FIELD_KO`/`_EN` 상수를 공유한다(툴마다 손으로 쓰면 갈라진다).

**데이터 무결성.** `data/*.pkl`과 프리셋은 pickle이라 로드 전 `data_integrity.EXPECTED_SHA256`과 대조한다. 데이터를 재생성했다면 `RUNART_ALLOW_UNVERIFIED_DATA=1`로 임시 우회한 뒤 **검수하고 새 체크섬을 코드에 반영**해야 한다. 운영에서는 우회 변수를 설정하지 않는다.

**`edge_is_runnable()`은 두 가지 일을 한다** — 라우팅 필터이자 **번들 프리셋 검증기**다. 여기를 조이면 체크섬으로 고정된 340개 프리셋이 깨진다. 새 라우팅 제약은 가중치 쪽에 넣어라(궁궐 제외는 `easy_route_weight`와 `precompute_weights`의 60배 가격으로 구현했고, 제거가 아니라 가격인 이유는 담장 안에 선 사용자도 밖으로 나올 수 있어야 하기 때문이다).

**라우팅 가중치는 문자열 속성이다.** `precompute_weights()`가 night × hills × parks 8가지 변형을 엣지에 구워 넣는다 — Dijkstra가 파이썬 함수 대신 dict 조회를 하게 하는 최대 지연 레버다. 편집 경로만 콜러블(`easy_route_weight`)을 쓴다. 새 라우팅 차원을 넣으면 변형이 2배가 된다.

**동물 프리셋은 그래프에 종속된다.** `animal_presets.graph_fingerprint()`가 그래프 파일에서 계산되므로, 그래프를 바꾸면 기존 프리셋은 자동 무효화된다. 그래프 재생성 시 `scripts/build_animal_presets.py`도 반드시 다시 돌린다.

**그래프가 없으면 데모 그리드로 뜬다.** `data/seoul_graph.pkl`이 없으면 시청 일대 합성 격자로 동작한다 — 테스트는 통과하지만 결과는 실제 서울이 아니다. 코스 품질을 판단하기 전에 실그래프 로드 여부를 확인할 것.

**외부 문자열은 이스케이프.** MCP Markdown 응답은 `render.markdown_text()`, HTML은 `html.escape()`를 거친다. 지오코딩 API 키·좌표는 로그·응답·에러에 남기지 않는다(uvicorn access log도 꺼져 있다).

**데이터 라이선스.** OSM(ODbL), 서울시 공공데이터(공공누리 1유형), SRTM을 사용한다. 데이터 출처를 추가·변경하면 `DATA_LICENSES.md`와 `/data-licenses` 페이지를 함께 갱신해야 한다.

## 코스 상세 웹페이지 (`render.py`)

한 코스가 세 페이지를 갖는다 — `/c/{id}`(코스 정보) · `/c/{id}/run`(달리기) · `/c/{id}/editor`(코스 편집). `preview_html(..., page=)` 하나가 셋을 만들고, **머리말·CSS·지도·탭바·스크립트는 완전히 동일**하다. 본문 섹션만 바뀐다. 스크립트를 페이지별로 쪼개지 않는다 — 모든 기능이 이미 자기 DOM 노드를 `if (!el) return`으로 방어하므로, 하나의 스크립트가 바인딩할 것을 못 찾는 편이 갈라진 셋보다 안전하다.

**`id="x"` 속성이 만드는 암묵적 전역(`window.x`)에 의존하지 말 것.** 그 요소가 어떤 페이지에서 렌더링되지 않는 순간 `ReferenceError`가 나고, `kakao.maps.load()` 콜백 안이면 **그 뒤에 바인딩되는 모든 핸들러가 통째로 죽는다**(증상: 버튼이 전부 무반응). 모든 컨트롤은 `document.getElementById`로 명시 선언하고, `tests/test_course_pages.py`의 가드 테스트가 이를 검사한다.

**`course_edit_summary()`와 `preview_html()`은 같은 것을 말해야 한다.** 편집 응답이 이 payload를 돌려주고 `applySummary()`가 패널을 다시 그린다. 페이지에 표시되는 값을 추가하면 이 둘과 JS 렌더러 세 곳을 함께 고쳐야 한다. 코스 id를 주소로 쓰는 링크(GPX·코스 카드·공유·탭)도 `setCourseLinks()`로 함께 따라가야 한다 — 편집된 코스가 원본 파일을 넘겨주던 버그가 있었다.

**Kakao Maps JS SDK 제약** — 지도 회전 API가 없다(`setBearing`/`setAngle` 부재). 그래서 달리기 모드는 북쪽 고정 + 마커 방향 콘 회전이다. JS 키는 도메인 제한이라 **배포 도메인에서만 지도가 뜬다**; 로컬에서 보려면 카카오 콘솔 Web 플랫폼에 `http://localhost:8000`을 등록해야 한다.

**`map.panBy()`는 애니메이션이다. 드래그에 쓰지 말 것.** pointermove마다 부르면 매 프레임 애니메이션이 새로 시작돼 지도가 몇 픽셀 움직이다 되돌아온다 — 코스 정보에서 "지도가 안 끌린다"의 정체였다(편의시설 마커가 최대 80개라 드래그 대부분이 마커 위에서 시작한다). 즉시 이동은 `panByPixels()`(projection + `setCenter`)를 쓴다. 마커의 터치 히트 영역도 44px(±9px)까지만 — ±16px(58px)면 마커들이 지도를 타일처럼 덮는다.

**세 페이지의 코스 선은 같은 좌표에서 나와야 한다.** 정보·달리기는 `_segments_with_rfs()`(=`graphmod.edge_points`, 실제 OSM 도로 형상)를 쓴다. 편집 화면도 `edit_path_geometry()`로 같은 형상을 받아 `drawnPoints()`로 그린다. 예전엔 편집만 노드 간 직선을 그려서 **같은 코스가 두 개의 선**을 가졌고, 그 직선이 블록을 가로질렀다. `geometry`가 없는 엣지는 실측상 100%가 실제로 직선이므로(4만개 표본, 오차 <2%) 직선 렌더링이 맞다.

**페이지 스크립트는 Node로 검증할 수 있다.** 지도 없이도 `node --check`로 문법을, DOM/SDK 스텁으로 `kakao.maps.load()` 콜백이 끝까지 도는지를 확인할 수 있다 — 위의 암묵적 전역 버그를 배포 없이 잡아낸 방법이다.

## 코스 편집 파이프라인

**그리기는 남아 있고, 입력 방식만 바뀌었다.** 자유 곡선(연필)은 없앴다 — 손가락은 카카오맵이 그려주지 않는 보행망을 따라 그릴 수 없어서 그린 선이 건물을 뚫는 경로로 돌아왔다. 지금은 두 동작 다 **잡고 끄는 제스처**이고, 둘 다 보행 그래프가 모든 미터를 그린다.

- **지우개**: 구간을 훑어 표시 → `지우기` → 서버 `reroute`가 **그 자리에서 다른 보행로로 다시 이어 준다**. 예전엔 빈 구간만 남기고 연필을 기다렸기 때문에, 튀어나온 선을 지우고 저장하면 **지우기 전 코스가 저장됐다**.
- **그리기(끌기)**: 코스 선을 **잡고 끌면** 그 구간이 손가락 위치를 지나도록 다시 이어진다(서버 `via`). 멀리 당기면 늘어나고, 제자리로 당기면 줄어든다. 탭은 길이 0인 끌기다.

**끌기 구간(`spanBase.range`)은 잡은 순간에 고정한다.** 당김 거리에 따라 넓혔더니 교체 경로가 우회로보다 더 많은 원본 구간을 삼켜서 **당길수록 코스가 짧아졌다**(312m 당김에 5.23km → 3.96km). 고정하면 같은 동작이 5.23km → 5.61km로 늘어난다.

**끌기 요청은 항상 한 건만 날린다.** 드래그는 네트워크가 답할 수 있는 것보다 훨씬 많은 샘플을 만들고, 늦게 온 응답이 최신 응답과 같은 경로를 두고 싸운다. `dragInFlight` 하나로 막고 최신 좌표만 trailing으로 보낸다. undo도 **드래그 전체가 한 단계**다(`spanBase.pushed`).

드래그 중 손가락은 강·건물 위를 지나간다. **중간 실패는 토스트를 띄우지 않고**(`job.final`) 손가락이 멈춘 곳에서만 알린다.

`snap` 액션은 남겨 뒀다. UI는 더 이상 보내지 않지만 브라우저에 열려 있는 예전 페이지가 보낼 수 있다(무상태 배포).

**지우개 범위는 선을 따라서만 자란다.** 순환 코스는 자기 자신과 가까이 지나므로, 전역 최근접 탐색은 코스 반대편 구간을 답으로 내놓고 그 사이 전부가 선택된다. `nearestSegment(point, lo, hi)`가 현재 선택 ±`ERASE_REACH`(24노드)로 탐색을 제한한다.

**대안이 없으면 잘라낸다.** 스퍼(왕복으로 튀어나온 선)의 양 끝을 잇는 다른 길은 정의상 없다. `collapse_excursion()`이 그 구간을 품는 가장 가까운 왕복 지점(같은 노드가 두 번 나오는 곳)을 찾아 통째로 잘라내고, 이유를 `course.note`로 알린다.

**"그렸는데 안 그려지는 곳"은 이유를 말한다.** `edge_is_runnable()`이 `steps`(2,753개)와 경사 10% 초과 `path`를 제외하므로, 계단 위를 짚으면 경로가 우회한다. `blocked_reason_near()`가 **가장 가까운 노드가 아니라 가장 가까운 엣지**를 본다 — 모든 계단은 보행로에서 시작하므로 노드 기준으로는 항상 "멀쩡한 길 있음"이 나온다. 멀쩡한 길이 8m 이내로 똑같이 가까우면 침묵한다(없는 핑계를 만들지 않는다).

**탭은 노드가 아니라 장소를 가리킨다.** 최근접 노드는 계단 밑·건물 안뜰처럼 라우터가 못 가는 곳인 경우가 흔하다. `route_via_points()`는 `nearby_nodes` 후보 6개를 순서대로 시도한 뒤에야 실패시킨다.

그린 선 처리 순서: **RDP 단순화(45m) → 경유지 최소 간격 140m·최대 8개 → 경유지별 최단경로 연결 → 왕복 구간 제거**. 각 단계에 이유가 있고, 넷 다 실제 버그에서 나왔다.

- 단순화·간격을 좁히면 손 흔들림이 경유지가 되어 **지그재그**가 생긴다(직선 489m가 1,515m·중복 10곳으로 나왔다).
- 왕복 제거에는 두 가지 예외가 있다. **같은 길을 두 번 그린 경우**(`stroke_is_doubled` — 근접만이 아니라 진행 방향이 뒤집혀야 하고, 그런 쌍이 4개 이상이어야 한다)와 **두 끝점을 잇는 직선에서 150m 이상 벗어난 경유지**(`_detour_nodes`)다. 후자가 없으면 사용자가 일부러 그린 우회로가 통째로 사라진다.
- 경유지가 고립 노드에 붙으면 그것만 건너뛴다. 하나 때문에 편집 전체를 실패시키지 않는다.
- 교체 결과가 원본과 동일하면 그 이유를 알린다 — 도로망에 **절단변(유일한 연결)**이 있어 대안이 아예 없는 경우가 실재한다.

`snap`에만 해당하던 RDP·왕복 제거 규칙은 그대로 남아 있지만, 현재 UI 경로(`reroute`/`via`)는 자유 곡선을 다루지 않으므로 지그재그가 생길 여지가 없다.

## 지도와 코스 선의 괴리

카카오맵은 이름 있는 도로를 그리고 이름 없는 보행로는 거의 그리지 않는다(실측: residential 92% / primary 98% vs footway 3% / path 12%). 그래서 이름 없는 `footway`·`path`에 2.2배 비용을 매겨 **지도에 보이는 길을 선호**한다. 공원 러닝은 예외라 `need_facilities=["park"]`가 이 벌점을 끄는 프로파일을 고른다.

카카오는 제3자에게 보행 도로 형상·경로 API를 주지 않으므로 **완전한 일치는 불가능하다**. 가중치는 대안이 있을 때만 작동한다. 그래프의 42%만 `geometry`를 갖지만 나머지는 실제로 곧은 길이므로(직선거리와 길이가 정확히 일치) 직선 렌더링은 정확하다 — 화면의 긴 대각선을 렌더링 버그로 오해하지 말 것.

**"코스 선이 도로 한가운데 있다"는 데이터 사실이지 버그가 아니다.** 실측(5km 코스 6개, 30.2km): primary 31% · residential 25% · secondary 23% · tertiary 13% = **길이의 91.5%가 도로 중심선**이다. OSM 서울에는 인도(sidewalk)가 별도 way로 매핑돼 있지 않고, 엣지 속성에도 `sidewalk`/`footway=sidewalk` 태그가 없다(`sidewalk_score`는 서울시 공공데이터 기반 점수이고 커버리지가 희박해 69%가 낮게 나온다). **어느 쪽 인도인지는 현재 데이터로 답할 수 없다.** 라우팅을 바꿔서 해결할 문제가 아니므로, 이 항목이 다시 올라오면 데이터 확보(서울시 보행자도로 GIS 등) 여부부터 따져라.

**공원 코스는 임의로 그려지지 않는다.** 실측(공원 코스 6개, 29.9km): 그래프에 없는 구간 0개, 공원 구간의 39%가 footway/path/pedestrian, 나머지는 공원 진입 도로(unclassified·service)다. 자유 곡선처럼 *보이는* 이유는 카카오가 그 보행로를 그리지 않기 때문이다(footway 중 이름 있는 것은 3%).
